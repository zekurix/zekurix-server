components:
  parameters:
    UserId:
      name: id
      in: path
      description: Unique identifier of the user.
      required: true
      schema:
        $ref: "./schemas.yaml#/components/schemas/UserId"
